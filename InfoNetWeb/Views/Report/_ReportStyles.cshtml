<style type="text/css">
	.icjia-report-group-title {
		font-size:16px;
	}
	tr {
		page-break-inside: avoid;
	}
	thead {
		page-break-inside: avoid;
		page-break-after: avoid;
	}
	tbody > tr:first-child {
		page-break-before: avoid;
	}
	tbody > tr:last-child {
		page-break-after: avoid;
	}
	tfoot {
		page-break-inside: avoid;
		page-break-before: avoid;
	}
	div.panel.panel-primary {
		margin-bottom: 60px;
	}
	.icjia-report-time {
		font-size:10px
	}
	.icjia-report-preheader {
		font-size:16px;
		font-weight:bold;
		margin-bottom:20px;
	}
	.table > tbody > tr.subtotal, .table > tbody > tr.grandtotal {
		border-top: 2px solid #ddd;
		border-bottom: 2px solid #ddd;
		background-color:#f2f2f2
	}
</style>